using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewUnit
{

    /*private IUnit _unit;

    public delegate void BonusDamage(GameObject toDamage, bool isDot); 
    public BonusDamage bonusDamage;
    
    public NewUnit(IUnit unit){
        _unit = unit;
    }

    public void UpdateStatusEffects(){
        _unit.statusEffects.UpdateEffects();
    }

    /*public void CheckForSkillLevelUpInput(){
        levelManager.LevelUpSkill();
    }*/

    /*public void CheckForDamageReset(float currentTime){
        damageTracker.CheckForReset(currentTime);
    }

    public float GetMoveSpeed(){
        return unitStats.CalculateMoveSpeed(statusEffects);
    }

    public void UpdateAttackSpeed(){
        unitStats.UpdateAttackSpeed();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
